# Benchmark Data

The full historical AdaptDQC benchmark QASM set is intentionally not included
in the Janus4 topic import. The attendee tutorial notebook does not read those
files; it only needs this directory as a repository marker while it runs small
local examples.

Full benchmark reproduction data should live in a release asset or external
artifact store rather than in the default tutorial source tree.
